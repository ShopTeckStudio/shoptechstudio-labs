services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
 
    ports:
      - "80:80"
# Unhash if using HTTPS
#      - "443:443"
 
    volumes:
      # Main Nginx config
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
 
      # Site-specific configs — drop .conf files in ./config/conf.d/
      - ./config/conf.d:/etc/nginx/conf.d:ro
 
      # Static web files
      - ./html:/usr/share/nginx/html:ro
 
      # SSL certificates (e.g. from Certbot/Let's Encrypt)
# Uncomment if using HTTPS and SSL     
#      - ./config/certs:/etc/nginx/certs:ro
      
      # Persistent logs
      - ./logs:/var/log/nginx
 
    networks:
      - proxy
 
networks:
  proxy:
    name: proxy
    driver: bridge
    # Other containers can join this network by adding:
    #   networks:
    #     - proxy
    # and will be reachable from Nginx by their container name
 
