HELPER NAME- Leak Alert Latched

Type- Toggle
Name-Leak Alert Latched

---------------------------------------------
HELPER NAME- Leak Sensor Count State On

Type-Template
Type-Sensor
State-Measurement

Edit .yml

{{ 
  states.binary_sensor
  | selectattr('attributes.device_class', 'eq', 'moisture')
  | selectattr('state', 'in', ['wet', 'on'])
  | list
  | count
}}

---------------------------------------------
HELPER NAME- Last Leak Sensor

Type- Text
Name: Last Leak Sensor
“Leave everything else default”
“Save”
