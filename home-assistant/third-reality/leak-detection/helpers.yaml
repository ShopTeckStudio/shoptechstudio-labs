# Helpers created via Home Assistant UI
# Settings → Devices & Services → Helpers

# =================================================
# LEAK ALERT LATCHED
# =================================================

# Toggle Helper
# Name: Leak Alert Latched
# Type: Toggle (input_boolean)

# =================================================
# LEAK SENSOR COUNT STATE ON
# =================================================

# Template Helper (Sensor)
# Name: Leak Sensor Count State On
# State class: Measurement

# Template used:

{{ 
  states.binary_sensor
  | selectattr('attributes.device_class', 'eq', 'moisture')
  | selectattr('state', 'in', ['wet', 'on'])
  | list
  | count
}}

# =================================================
# LAST LEAK SENSOR
# =================================================

# Text Helper
# Name: Last Leak Sensor
# Type: Text (input_text)
# Leave all other options default
